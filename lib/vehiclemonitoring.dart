import 'package:flutter/material.dart';

class Vehiclemonitoring extends StatelessWidget {
  const Vehiclemonitoring({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vehicle Monitoring'),
      ),
    );
  }
}
