import 'package:flutter/material.dart';

class Leavefilling extends StatelessWidget {
  const Leavefilling({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Leave Filling'),
      ),
    );
  }
}
